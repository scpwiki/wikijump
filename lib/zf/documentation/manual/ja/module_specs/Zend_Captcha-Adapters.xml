<!-- EN-Revision: 12740 -->
<sect1 id="zend.captcha.adapters">
    <title>Captcha アダプタ</title>

    <para>
        次のアダプタが、Zend Framework に同梱されています。
    </para>

    <sect2 id="zend.captcha.adapters.word">
        <title>Zend_Captcha_Word</title>

        <para>
            Zend_Captcha_Word は抽象アダプタで、
            Dumb、Figlet および Image アダプタの基底アダプタとなります。
            指定できる機能は、単語の長さやセッションの有効期限、
            使用するセッション名前空間オブジェクト、
            Zend_Session_Namespace を使いたくない場合に使用するセッション名前空間クラスです。
            さらに、すべての検証ロジックをカプセル化します。
        </para>

        <para>
            デフォルトでは、単語の長さは 8 文字です。またセッションのタイムアウトは
            5 分、情報の永続化には Zend_Session_Namespace を使用します
            (使用する名前空間は "Zend_Form_Captcha_&lt;captcha ID&gt;" です)。
        </para>

        <para>
            <code>Zend_Captcha_Adapter</code> インターフェイスの標準メソッドのほかに、
            <code>Zend_Captcha_Word</code> は次のメソッドを公開しています。
        </para>

        <itemizedlist>
            <listitem><para>
                <code>setWordLen($length)</code> と <code>getWordLen()</code>
                で、生成される "単語" の文字数を設定したり現在の値を取得したりします。
            </para></listitem>

            <listitem><para>
                <code>setTimeout($ttl)</code> と <code>getTimeout()</code>
                で、セッショントークンの有効期限を設定したり現在の値を取得したりします。
                <code>$ttl</code> は秒数で指定します。
            </para></listitem>

            <listitem><para>
                <code>setSessionClass($class)</code> と
                <code>getSessionClass()</code> で、captcha トークンの永続化に使用する
                <code>Zend_Session_Namespace</code> の実装を設定したり
                現在の値を取得したりします。
            </para></listitem>

            <listitem><para>
                <code>getId()</code> で、現在のトークン識別子を取得します。
            </para></listitem>

            <listitem><para>
                <code>getWord()</code> で、captcha
                に使用するために生成した単語を取得します。
                まだ生成されていない場合は、まず生成してからそれを返します。
            </para></listitem>

            <listitem><para>
                <code>setSession(Zend_Session_Namespace $session)</code>
                で、captcha トークンの永続化に使用するセッションオブジェクトを設定します。
                <code>getSession()</code> で、現在のセッションオブジェクトを取得します。
            </para></listitem>
        </itemizedlist>

        <para>
            すべての Word captcha は、コンストラクタにオプションの配列を渡すことができます。
            別の方法として、その配列を <code>setOptions()</code>
            で渡す (あるいは <code>Zend_Config</code> オブジェクトを
            <code>setConfig()</code> で渡す) こともできます。
            デフォルトで、
            <code>wordLen</code>、<code>timeout</code> および
            <code>sessionClass</code> のキーをすべて使用します。
            各具象実装では、それ以外のキーを使用したり
            違う方法でオプションを使用したりしているかもしれません。
        </para>

        <note>
        <para>
            Word は抽象クラスであり、直接そのインスタンスを作成することはできません。
        </para>
        </note>
    </sect2>

    <sect2 id="zend.captcha.adapters.dumb">
        <title>Zend_Captcha_Dumb</title>

        <para>
            Dumb アダプタは、その名が示すとおりのものです。
            ランダムな文字列を用意し、それを逆からタイプさせることで検証を行います。
            これは CAPTCHA の手法としてはあまりよいものではないので、
            テスト用に使うのみにしておきましょう。
            あるいは、ほかに手がない場合の最後の手段としてのみ使うようにしましょう。
            このアダプタは <code>Zend_Captcha_Word</code> を継承しています。
        </para>
    </sect2>

    <sect2 id="zend.captcha.adapters.figlet">
        <title>Zend_Captcha_Figlet</title>

        <para>
            Figlet アダプタは、<link
                linkend="zend.text.figlet">Zend_Text_Figlet</link>
            を使用して Figlet をユーザに表示します。
            Figlet による captcha は文字しか使用できません。
        </para>

        <para>
            コンストラクタに渡されたオプションは、アダプタが使用する <link
                linkend="zend.text.figlet">Zend_Text_Figlet</link>
            オブジェクトにも渡されます。
            使用できる設定オプションについては、そちらのドキュメントも参照ください。
        </para>
    </sect2>

    <sect2 id="zend.captcha.adapters.image">
        <title>Zend_Captcha_Image</title>

        <para>
            Image アダプタは、生成された単語を受け取ってそれを画像としてレンダリングし、
            それをいろいろな方法で歪めて自動判読を困難にします。
            これを使用するには、
            <ulink url="http://php.net/gd">GD 拡張モジュール</ulink>
            を TrueType あるいは Freetype のサポートつきでコンパイルする必要があります。
            現在、Image アダプタが生成できるのは PNG 画像のみです。
        </para>

        <para>
            <code>Zend_Captcha_Image</code> は
            <code>Zend_Captcha_Word</code> を継承しており、
            さらに次のメソッドを公開しています。
        </para>

        <itemizedlist>
            <listitem><para>
                <code>setExpiration($expiration)</code> と
                <code>getExpiration()</code> で、
                captcha 画像をファイルシステム上に残す期間を設定します。
                通常、これはセッションの有効期間より長くします。
                captcha オブジェクトが起動されるたびにガベージコレクションが働き、
                期限切れとなった画像が削除されます。
                値は秒数で指定します。
            </para></listitem>

            <listitem><para>
                <code>setGcFreq($gcFreq)</code> と <code>getGcFreg()</code>
                で、ガベージコレクションが働く頻度を設定します。ガベージコレクションは、
                <code>1/$gcFreq</code> (デフォルトは 100) 回のコールごとに実行されます。
            </para></listitem>

            <listitem><para>
                <code>setFont($font)</code> と <code>getFont()</code>
                で、使用するフォントを指定します。<code>$font</code> には、
                使用するフォントのパスをフルパス形式で指定する必要があります。
                この値を設定しなければ、captcha の生成時に例外がスローされます。
                フォントは必須です。
            </para></listitem>

            <listitem><para>
                <code>setFontSize($fsize)</code> と <code>getFontSize()</code>
                で、captcha を生成する際に使用するフォントのサイズをピクセル単位で設定します。
                デフォルトは 24px です。
            </para></listitem>

            <listitem><para>
                <code>setHeight($height)</code> と <code>getHeight()</code>
                で、生成される captcha 画像の高さをピクセル単位で指定します。
                デフォルトは 50px です。
            </para></listitem>

            <listitem><para>
                <code>setWidth($width)</code> と <code>getWidth()</code>
                で、生成される captcha 画像の幅をピクセル単位で指定します。
                デフォルトは 200px です。
            </para></listitem>

            <listitem><para>
                <code>setImgDir($imgDir)</code> と <code>getImgDir()</code>
                で、captcha 画像を保存するディレクトリを指定します。
                デフォルトは "./images/captcha/"
                で、これは起動スクリプトからの相対パスとなります。
            </para></listitem>

            <listitem><para>
                <code>setImgUrl($imgUrl)</code> と <code>getImgUrl()</code>
                で、HTML マークアップ時に使用する captcha 画像への相対パスを指定します。
                デフォルトは "/images/captcha/" です。
            </para></listitem>

            <listitem><para>
                <code>setSuffix($suffix)</code> と <code>getSuffix()</code>
                で、使用するファイル名の拡張子を指定します。デフォルトは
                ".png" です。注意: これを変更したからといって、
                生成される画像の形式が変わるわけではありません。
            </para></listitem>
        </itemizedlist>

        <para>
            上のすべてのオプションは、コンストラクタのオプションとして指定できます。
            その際には、メソッド名の先頭の 'set' を取り除いて先頭を小文字にした名前
            ("suffix", "height", "imgUrl" など) を使用します。
        </para>
    </sect2>

    <sect2 id="zend.captcha.adapters.recaptcha">
        <title>Zend_Captcha_ReCaptcha</title>

        <para>
            ReCaptcha アダプタは、<link
                linkend="zend.service.recaptcha">Zend_Service_ReCaptcha</link>
            を使用して captcha の生成と検証を行います。
            次のメソッドを公開しています。
        </para>

        <itemizedlist>
            <listitem><para>
                <code>setPrivKey($key)</code> と <code>getPrivKey()</code>
                で、ReCaptcha サービスで使用する秘密鍵を指定します。
                これはオブジェクトの作成時に指定する必要がありますが、
                その後いつでも上書きすることができます。
            </para></listitem>

            <listitem><para>
                <code>setPubKey($key)</code> と <code>getPubKey()</code>
                で、ReCaptcha サービスで使用する公開鍵を指定します。
                これはオブジェクトの作成時に指定する必要がありますが、
                その後いつでも上書きすることができます。
            </para></listitem>

            <listitem><para>
                <code>setService(Zend_Service_ReCaptcha $service)</code> と
                <code>getService()</code> で、
                ReCaptcha サービスオブジェクトを取得したり取得したりします。
            </para></listitem>
        </itemizedlist>
    </sect2>
</sect1>
<!--
vim:se ts=4 sw=4 et:
-->
