<!-- EN-Revision: 11089 -->
<sect1 id="zend.json.basics">
    <title>Utilisation de base</title>

    <para>L'utilisation de <code>Zend_Json</code> implique l'emploi des deux méthodes statiques publiques disponibles :
    <code>Zend_Json::encode()</code> et <code>Zend_Json::decode()</code>. <programlisting role="php"><![CDATA[
// Obtention d'une valeur
$phpNatif = Zend_Json::decode($valeurCodee);

// Codage pour renvoi au client :
$json = Zend_Json::encode($phpNatif);
]]></programlisting></para>
</sect1>