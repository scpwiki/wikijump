<!-- EN-Revision: 12116 -->
<sect1 id="zend.captcha.operation">
    <title>Opération Captcha</title>

    <para>Tous les objets concrets CAPTCHA implémentent <code>Zend_Captcha_Adapter</code>, qui ressemble à ceci :</para>

    <programlisting role="php"><![CDATA[
interface Zend_Captcha_Adapter extends Zend_Validate_Interface
{
    public function generate();

    public function render(Zend_View $view, $element = null);

    public function setName($name);

    public function getName();

    public function getDecorator();

    // De plus, pour satisfaire Zend_Validate_Interface :
    public function isValid($value);

    public function getMessages();

    public function getErrors();
}
]]></programlisting>

    <para>Le mutateur et l'accesseur "name" sont utilisés pour spécifier et récupérer l'identifiant du captcha.
    <code>getDecorator()</code> peut être utilisé pour spécifier un décorateur <code>Zend_Form</code> soit par son nom
    ou en retournant un objet décorateur. Les vraies clés sont utilisées sauf pour <code>generate()</code> et
    <code>render()</code>. <code>generate()</code> est utilisé pour créer l'élément captcha. Ce processus typiquement
    stockera l'élément en session ainsi il pourra être utilisé pour comparaison dans les requêtes suivantes.
    <code>render()</code> est utilisé pour effectuer le rendu de l'information que représente le captcha - en image, en
    texte Figlet, en problème logique, etc.</para>

    <para>Un cas d'utilisation typique pourrait ressembler à ceci :</para>

    <programlisting role="php"><![CDATA[
// Créer une instance de Zend_View
$view = new Zend_View();

// Requête original :
$captcha = new Zend_Captcha_Figlet(array(
    'name' => 'foo',
    'wordLen' => 6,
    'timeout' => 300,
));
$id = $captcha->generate();
echo $captcha->render($view);

// Lors de la requête suivante :
// suppose que $captcha a été paramètré avant,
// et que $value est la valeur soumise :
if ($captcha->isValid($_POST['foo'], $_POST)) {
    // Validated!
}
]]></programlisting>
</sect1>