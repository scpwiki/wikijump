// TODO: this css import is temporary
// it's just for the sake of making sure this css always gets loaded
// but if we were to make this, say, a NPM module, it's probably better to just
// leave a convinent way to import the css file rather than force it to be loaded
import "@root/ftml/misc/ftml-base.css"

export * from "./components/code"
